<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.PomodoroTimer" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize theme attributes as needed -->
    </style>
    
    <style name="CustomTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <style name="Theme.Splash" parent="Theme.PomodoroTimer">

    </style>

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/lilla</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/Theme.PomodoroTimer</item>

    </style>
</resources>
